import dashboard from './dashboard';
import other from './other';
import reports from './report';
import settings from './settings';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, other, settings, reports]
};

export default menuItems;
